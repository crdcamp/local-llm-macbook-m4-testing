# General Overvieqw

To begin with, llama-bench ships with llama.cpp, so don't worry about needing to install it or any of that bs.

A quick reminder that models (on Mac at least) are stored in the following directory:

`~/.cache/huggingface/hub/`

You'll need this for pointing these commands to the models (duh).
